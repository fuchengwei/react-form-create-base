import React from 'react'
import ReactDOM from 'react-dom'
import { DesignForm } from '@/core/index'
import 'antd/dist/antd.css'

ReactDOM.render(
  <React.StrictMode>
    <DesignForm />
  </React.StrictMode>,
  document.getElementById('root')
)
